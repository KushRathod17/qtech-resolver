fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 calls bcrypt.__about__, removed in bcrypt 4.1. Anything newer
# raises "password cannot be longer than 72 bytes" on every hash() call.
bcrypt==4.0.1
python-multipart==0.0.12
pydantic[email]==2.9.2
pydantic-settings==2.5.2
alembic==1.13.3
python-dotenv==1.0.1
# S3-compatible object storage for attachments/avatars in production (see
# app/storage.py) -- only imported if S3_* env vars are set, but always
# installed so a fresh prod deploy never fails on a missing dependency.
boto3==1.35.36
# Pure-Python PDF generation for the Reports page's "Export PDF" button
# (app/pdf_report.py) -- no system-level dependency (unlike wkhtmltopdf/weasyprint),
# so it works unmodified on Render's free tier.
reportlab==4.2.5