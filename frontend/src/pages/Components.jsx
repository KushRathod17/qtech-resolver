// Dead file — the Components concept was migrated to a fixed Product string
// on Ticket (see the c3f9a71e5b2d migration and frontend/src/board/constants.jsx).
// No longer imported by App.jsx or Sidebar.jsx; safe to delete by hand
// (this sandbox's shell doesn't have permission to remove it from disk).
export default function ComponentsDeprecated() {
  return null;
}
